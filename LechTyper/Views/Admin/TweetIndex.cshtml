@model IPagedList<LechTyper.Models.Tweet>
@{
    ViewBag.Title = "Zarządzaj typami";
}
<hgroup class="title">
    <h1>@ViewBag.Title.</h1>
</hgroup>
<section class="panel">
    <p>
        @Html.ActionLink("Panel admina", "Admin")
    </p>
    <table class="table table-striped table-hover ">
        <thead>
            <tr>
                <th>Utworzony</th>
                <th>ID wpisu</th>
                <th>Typ</th>
                <th>ID użytkownika</th>
                <th>Nazwa</th>
                <th>Nick</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var tweet in Model)
            {
                <tr>
                    <td>@tweet.created_at</td>
                    <td>@tweet.post_id</td>
                    <td>@tweet.text</td>
                    <td>@tweet.user_id</td>
                    <td>@tweet.user_name</td>
                    <td>@tweet.user_nick</td>
                    <td>@Html.ActionLink("Edytuj", "TweetEdit", new { x = tweet.tweetid, page = Model.PageNumber })</td>
                    <td>@Html.ActionLink("Usuń", "TweetDelete", new { x = tweet.tweetid, page = Model.PageNumber })</td>
                </tr>
            }
        </tbody>
    </table>
    <div class="pager">
        @Html.Pager(Model.PageSize, Model.PageNumber, Model.TotalItemCount).Options(o => o
            .DisplayTemplate("BootstrapPagination"))
    </div>
</section>